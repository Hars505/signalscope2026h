# Django Settings
SECRET_KEY=django-insecure-change-me-in-production-signalscope-2026
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Optional model runtime controls
# ML models are loaded from the checked-in ModelsP2 and VIT_Model directories.
# Set to False only when running a lightweight backend without model dependencies.
SIGNALSCOPE_ENABLE_ML=True
